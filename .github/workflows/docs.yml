name: Publish Documentation

on:
  push:
    branches:
      - main
      - dev
    paths:
      - docs/**
      - mkdocs.yml
      - pyproject.toml
      - .github/workflows/docs.yml

  workflow_dispatch:

jobs:
  build:
    name: Build Documentation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set Up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11.1

      - name: Set Up Poetry
        run: pipx install poetry==1.4.1 && pipx install poethepoet

      - name: Configure Material Credentials
        run: |
          poetry config repositories.material https://github.com/squidfunk/mkdocs-material-insiders
          poetry config http-basic.material celsiusnarhwal ${{ secrets.MATERIAL_INSIDERS_TOKEN }}

      - name: Save Cache
        uses: actions/cache@v2
        with:
          key: ${{ github.ref }}
          path: .cache

      - name: Build Documentation
        run: poetry install --no-root --only docs && poe fontawesome && poetry run mkdocs build
        env:
          MKDOCS_GIT_COMMITTERS: ${{ github.ref == 'refs/heads/main' && 'true' || 'false' }}
          FONTAWESOME_NPM_AUTH_TOKEN: ${{ secrets.FONTAWESOME_NPM_AUTH_TOKEN }}

      - name: Upload Artifact (Production)
        if: ${{ github.ref == 'refs/heads/main' }}
        uses: actions/upload-pages-artifact@v1
        with:
          path: site

      - name: Upload Artifact (Preview)
        if: ${{ github.ref == 'refs/heads/dev' }}
        uses: actions/upload-pages-artifact@v1
        with:
          path: site
          name: preview

  deploy-prod:
    name: Deploy Documentation
    runs-on: ubuntu-latest
    needs: build
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        if: ${{ github.ref == 'refs/heads/main' }}
        uses: actions/deploy-pages@v1

  deploy-preview:
    name: Deploy Preview Documentation
    runs-on: ubuntu-latest
    needs: build
    permissions:
      deployments: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Deploy to Vercel
        if: ${{ github.ref == 'refs/heads/dev' }}
        uses: s0/git-publish-subdir-action@develop
        env:
          REPO: self
          BRANCH: vercel
          FOLDER: site
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
